os:
  editPreset: "nvim"

customCommands:
  - key: "P"
    context: "global"
    description: "Push in background"
    command: >-
      zsh -lc 'launchctl submit -l "git-push-bg-$(date +%s)-$$" -- /bin/zsh "$HOME/.config/lazygit/push-bg.zsh" "$PWD" >/dev/null 2>&1'
